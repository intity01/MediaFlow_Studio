# MediaFlow Configuration
# 
# Copy this file to .env.local before running:
#   cp .env.example .env.local
#
# ⚠️ NEVER commit .env.local to version control

# ===========================================
# NO API KEYS REQUIRED!
# ===========================================
# MediaFlow runs 100% locally on your computer.
# All processing is done using local tools:
# - yt-dlp for downloading
# - FFmpeg for audio/video processing  
# - audio-separator for stem separation (Python)
# - PyTorch for GPU acceleration
#
# No cloud services, no API keys, no data collection.

# Optional: Custom model cache directory
# MODEL_CACHE_DIR=/path/to/models

# Optional: Default output directory
# OUTPUT_DIR=/path/to/output
